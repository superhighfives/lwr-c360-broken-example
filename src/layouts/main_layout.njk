<!doctype html>
<html lang="en">
	<head>
		<meta charset="utf-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
		<title></title>
        <link rel="stylesheet" href="$assetsDir/css/main.css" />
        <link rel="stylesheet" href="$assetsDir/css/hooks.custom-props.css" />
	</head>

	<body>
        {{ body | safe }}
        {{ lwr_resources }}
	</body>
</html>
